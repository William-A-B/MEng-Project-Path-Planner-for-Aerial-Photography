% Sample script to demonstrate execution of function waypoints = lawnmowerPath(areaWidth, areaHeight, swathWidth, startX, startY)
areaWidth = 100; % Initialize areaWidth here
areaHeight = 200; % Initialize areaHeight here
swathWidth = 20; % Initialize swathWidth here
startX = 0; % Initialize startX here
startY = 0; % Initialize startY here
waypoints = lawnmowerPath(areaWidth, areaHeight, swathWidth, startX, startY);
